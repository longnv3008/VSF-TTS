# Changelog

## 2026-05-29

- Tao he thong `docs/` moi toi uu cho AI va nguoi moi vao repo.
- Bo sung `ai-context/` de AI doc nhanh bo nho du an, repo map, work rules.
- Chia tai lieu theo `phases/` gan voi workflow hien tai cua audio pipeline.
- Them `decisions/`, `runs/`, `changelog/` de luu tri thuc va handoff ve sau.
- Bo sung guard rail cho YouTube crawl: global crawl lock, inter-url delay, retry/backoff, cache skip video da tai.
- Doi subtitle crawl sang chi lay `vi`, bo `en` de giam file raw thua.
- Chuyen group job tu `batch_name` sang bang `pipeline_batches` + `batch_id`, bo sung API doc batch va `resume-batch`.

## Cach dung

Moi thay doi lon nen them 1 muc ngan theo ngay:

- thay doi kien truc
- thay doi workflow
- thay doi API
- thay doi dashboard hoac van hanh
